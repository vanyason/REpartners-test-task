services:
    server:
        build: .
        ports:
            - "8080:8080"
        environment:
            - ADDR=:8080
            - PACK_FILE=/app/data/packs.json
        volumes:
            - pack-data:/app/data
        restart: unless-stopped

volumes:
    pack-data:
